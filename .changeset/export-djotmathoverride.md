---
"mathlifier": minor
"svelte-djot-math": patch
---

Export `djotMathOverride` function from `mathlifier` package. This allows users to use the djot math override directly when rendering djot content with custom HTML renderers.

Also consolidates import style in `svelte-djot-math` for consistency.
